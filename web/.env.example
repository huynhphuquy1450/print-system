# Mặc định để trống — web admin gọi API cùng origin (same-origin, dùng đường dẫn tương đối).
# Chỉ set biến này khi web và API deploy khác origin (split-origin).
# VITE_API_BASE_URL=http://localhost:3000
