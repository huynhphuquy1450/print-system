{
  "name": "print-agent",
  "version": "1.0.0",
  "description": "Print agent - subscribes MQTT, prints via SumatraPDF, callbacks status to Print Service",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.5",
    "mqtt": "^5.10.1"
  }
}
